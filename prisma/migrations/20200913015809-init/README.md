# Migration `20200913015809-init`

This migration has been generated by Saggitarie at 9/13/2020, 10:58:09 AM.
You can check out the [state of the schema](./schema.prisma) after the migration.

## Database Steps

```sql
CREATE TABLE "public"."User" (
"id" text   NOT NULL ,
"username" text   NOT NULL ,
"password" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."DrinkingLocations" (
"id" text   NOT NULL ,
"locationName" text   NOT NULL ,
"longtitude" integer   NOT NULL ,
"latitude" integer   NOT NULL ,
"createdAt" timestamp(3)   NOT NULL DEFAULT CURRENT_TIMESTAMP,
"userId" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE TABLE "public"."Profile" (
"id" text   NOT NULL ,
"image" text   NOT NULL ,
"userId" text   NOT NULL ,
PRIMARY KEY ("id")
)

CREATE UNIQUE INDEX "DrinkingLocations_userId_unique" ON "public"."DrinkingLocations"("userId")

CREATE UNIQUE INDEX "Profile_userId_unique" ON "public"."Profile"("userId")

ALTER TABLE "public"."DrinkingLocations" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE

ALTER TABLE "public"."Profile" ADD FOREIGN KEY ("userId")REFERENCES "public"."User"("id") ON DELETE CASCADE ON UPDATE CASCADE
```

## Changes

```diff
diff --git schema.prisma schema.prisma
migration ..20200913015809-init
--- datamodel.dml
+++ datamodel.dml
@@ -1,0 +1,36 @@
+// This is your Prisma schema file,
+// learn more about it in the docs: https://pris.ly/d/prisma-schema
+
+datasource db {
+  provider = "postgresql"
+  url = "***"
+}
+
+generator client {
+  provider = "prisma-client-js"
+}
+
+model User {
+  id               String            @default(cuid()) @id
+  username         String
+  password         String
+  drinkingLocation DrinkingLocations
+  profile          Profile
+}
+
+model DrinkingLocations {
+  id           String   @default(cuid()) @id
+  locationName String
+  longtitude   Int
+  latitude     Int
+  createdAt    DateTime @default(now())
+  user         User     @relation(fields: [userId], references: [id])
+  userId       String
+}
+
+model Profile {
+  id     String @default(cuid()) @id
+  image  String
+  user   User   @relation(fields: [userId], references: [id])
+  userId String
+}
```


